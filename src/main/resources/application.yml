server:
  port: 8761
  
eureka:
  instance:
   hostname: eureka-server.mysql1.svc
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
    register-with-eureka: true
    fetch-registry: true